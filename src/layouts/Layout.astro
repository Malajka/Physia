---
import Nav from "@/components/nav/Nav.astro";
import "../styles/global.css";

const { title = "Physia - Exercise Plans" } = Astro.props;

// Check if user is authenticated
const {
  data: { session },
} = await Astro.locals.supabase.auth.getSession();
const isAuthenticated = !!session;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charSet="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/ico" href="/favicon.ico" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body className="min-h-screen bg-[var(--background)] text-[var(--foreground)]">
    <header className="bg-[var(--card)] shadow-md">
      <div className="container mx-auto px-2 sm:px-4 py-3 flex items-center">
        <a href="/" className="flex items-center gap-2">
          <img src="/favicon.ico" alt="Physia logo" className="h-8 w-auto" />
          <span className="text-2xl font-bold text-[var(--primary)]">Physia</span>
        </a>
        <div className="flex-1 flex items-center">
          <Nav isAuthenticated={isAuthenticated} />
        </div>
      </div>
    </header>

    <main>
      <slot />
    </main>

    <footer className="mt-8 py-6 bg-[var(--muted)] text-center text-sm text-[var(--gray-text)]">
      <div className="container mx-auto px-4">
        <p>&copy; {new Date().getFullYear()} Physia. All rights reserved.</p>
      </div>
    </footer>
  </body>
</html>

<style>
  html,
  body {
    margin: 0;
    width: 100%;
    height: 100%;
  }
</style>
