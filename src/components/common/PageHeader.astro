---
export interface Props {
  title: string;
  subtitle?: string;
}
const { title, subtitle }: Props = Astro.props;
---

<header class="mb-6">
  <h1 class="text-2xl font-bold">{title}</h1>
  {subtitle && <p class="text-gray-600 mt-2 text-sm">{subtitle}</p>}
</header>
